from dlf.tensor import Tensor
from dlf.optimizer import SGD, get_parameters

class Network:
    def __init__(self):
        return

    def __call__(self, x: Tensor):
        l1 = Tensor(y) * x
        l2 = Tensor(b) + l1
        return l2

model = Network()
