[project]
name = "dlf"
version = "0.1.0"
description = "deep learning framework"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "polars",
    "matplotlib",
    "safetensors",
]

[tool.ruff]
line-length = 250

[tool.ruff.format]
skip-magic-trailing-comma = true
